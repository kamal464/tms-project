import { IntdatetimePipe } from './intdatetime.pipe';

describe('IntdatetimePipe', () => {
  it('create an instance', () => {
    const pipe = new IntdatetimePipe();
    expect(pipe).toBeTruthy();
  });
});
