import { StringdatetimePipe } from './stringdatetime.pipe';

describe('StringdatetimePipe', () => {
  it('create an instance', () => {
    const pipe = new StringdatetimePipe();
    expect(pipe).toBeTruthy();
  });
});
